package com.bluemoon.backend.enums;

/**
 * Enum for resident relationship to the household head.
 */
public enum ResidentRelationship {
    OWNER,
    SPOUSE,
    CHILD,
    PARENT,
    SIBLING,
    RELATIVE,
    OTHER
}
